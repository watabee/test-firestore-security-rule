{
  "name": "test-firestore-security-rule",
  "version": "1.0.0",
  "scripts": {
    "test": "mocha --timeout=10000"
  },
  "devDependencies": {
    "@firebase/testing": "^0.16.14",
    "mocha": "^7.1.0"
  }
}
